<Window x:Class="TaskTrackerApp.WidgetView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:TaskTrackerApp"
        Title="Task And Ticket Tracker" Height="200" Width="450"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ResizeMode="NoResize" ShowInTaskbar="False"
        WindowStartupLocation="Manual" Icon="pack://application:,,,/app.ico"
        MinWidth="300" MinHeight="150"
        LocationChanged="Window_LocationChanged" Loaded="Window_Loaded">
    <Window.Resources>
        <local:StringToVisibilityConverter x:Key="StringToVisConverter"/>
        
        <!-- Style for Window Control Buttons (Expand/Close) -->
        <Style x:Key="WindowControlBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#888888"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#33FFFFFF"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Specific Close Button Style -->
        <Style x:Key="CloseBtnStyle" TargetType="Button" BasedOn="{StaticResource WindowControlBtn}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#33FF0000"/>
                    <Setter Property="Foreground" Value="#FF4444"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Invisible/Minimal ScrollBar -->
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Border Background="{TemplateBinding Background}" CornerRadius="2">
                            <Track x:Name="PART_Track" IsDirectionReversed="true">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border Background="#33FFFFFF" CornerRadius="2"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Chevron Style -->
        <Style x:Key="ChevronBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Opacity" Value="0.5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="1"/>
                                <Setter Property="Background" Value="#22FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Link Button Style for Done/Skip -->
        <Style x:Key="LinkBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent" Padding="8,4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <!-- Transparent Background with subtle shadow -->
    <Border x:Name="MainBorder" Background="#F21A1D24" CornerRadius="8" Margin="8"
            MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="2" BlurRadius="10" Opacity="0.5" />
        </Border.Effect>
        
        <Grid Margin="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Button x:Name="VstsBadgeButton" Grid.Column="0" Background="#332196F3" BorderThickness="0" Padding="4,2" VerticalAlignment="Center" Click="VstsBadgeButton_Click" Cursor="Hand" ToolTip="Open ticket">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                <ContentPresenter/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <TextBlock x:Name="ActiveVstsTextBlock" Foreground="#64B5F6" FontSize="11" FontWeight="Bold"/>
                </Button>
                
                <!-- Drag Handle -->
                <TextBlock x:Name="DragHandle" Grid.Column="1" Text="⋮⋮" Foreground="#555555" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14" Cursor="SizeAll" ToolTip="Drag widget">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Opacity" Value="0"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}}" Value="True">
                                    <Setter Property="Opacity" Value="1"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="⛶" Click="MaximizeButton_Click" Style="{StaticResource WindowControlBtn}" ToolTip="Open Manager" FontSize="12" Margin="0,0,2,0" Padding="4,2"/>
                    <Button Content="✕" Click="HideButton_Click" Style="{StaticResource CloseBtnStyle}" ToolTip="Minimize to Tray" FontSize="10" Padding="4,2"/>
                </StackPanel>
            </Grid>
            
            <!-- Task Display (Carousel) -->
            <Grid Grid.Row="1" Margin="0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="PrevButton" Grid.Column="0" Click="PrevButton_Click" Visibility="Collapsed" Style="{StaticResource ChevronBtn}" Padding="4">
                    <Path Data="M15.41 16.59L10.83 12l4.58-4.59L14 6l-6 6 6 6 1.41-1.41z" Fill="White" Stretch="Uniform" Width="12" Height="12"/>
                </Button>
                
                <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" VerticalAlignment="Center" Margin="5,0">
                    <StackPanel>
                        <TextBlock x:Name="ActiveTaskTextBlock" Foreground="White" TextWrapping="Wrap" FontSize="20" FontWeight="Bold" Text="No active task selected." HorizontalAlignment="Center" TextAlignment="Center" Margin="0,0,0,5">
                            <TextBlock.Effect>
                                <DropShadowEffect ShadowDepth="1" BlurRadius="4" Opacity="0.8"/>
                            </TextBlock.Effect>
                        </TextBlock>
                        
                        <Grid x:Name="StepDisplayGrid" Visibility="Collapsed" Margin="0,5,0,0" HorizontalAlignment="Center" MaxWidth="350">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <Button x:Name="PrevStepButton" Grid.Column="0" Click="PrevStepButton_Click" Style="{StaticResource ChevronBtn}" Padding="4" VerticalAlignment="Center">
                                <TextBlock Text="&#xE76B;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Foreground="White" FontSize="12"/>
                            </Button>
                            
                            <Border Grid.Column="1" Background="#11FFFFFF" BorderBrush="#33FFFFFF" BorderThickness="1" CornerRadius="12" Padding="8,4" Margin="2,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <CheckBox x:Name="WidgetStepCheckBox" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,6,0" Click="WidgetStepCheckBox_Click">
                                        <CheckBox.LayoutTransform>
                                            <ScaleTransform ScaleX="0.8" ScaleY="0.8"/>
                                        </CheckBox.LayoutTransform>
                                    </CheckBox>
                                    <TextBlock x:Name="WidgetStepTextBlock" Grid.Column="1" Foreground="#EEEEEE" FontSize="13" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>

                            <Button x:Name="NextStepButton" Grid.Column="2" Click="NextStepButton_Click" Style="{StaticResource ChevronBtn}" Padding="4" VerticalAlignment="Center">
                                <TextBlock Text="&#xE76C;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Foreground="White" FontSize="12"/>
                            </Button>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
                
                <Button x:Name="NextButton" Grid.Column="2" Click="NextButton_Click" Visibility="Collapsed" Style="{StaticResource ChevronBtn}" Padding="4">
                    <Path Data="M8.59 16.59L13.17 12 8.59 7.41 10 6l6 6-6 6-1.41-1.41z" Fill="White" Stretch="Uniform" Width="12" Height="12"/>
                </Button>
            </Grid>
                     
            <!-- Footer -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,5">
                <Button x:Name="DoneButton" ToolTip="Mark as done" ToolTipService.InitialShowDelay="0" Foreground="#4CAF50" Style="{StaticResource LinkBtn}" Click="DoneButton_Click" Visibility="Collapsed" Margin="0,0,6,0" Padding="2">
                    <Path Data="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Stretch="Uniform" Width="12" Height="12"/>
                </Button>
                <Button x:Name="ResetButton" ToolTip="Reset task" ToolTipService.InitialShowDelay="0" Foreground="#888888" Style="{StaticResource LinkBtn}" Click="ResetButton_Click" Margin="0,0,6,0" Padding="2">
                    <Path Data="M17.65 6.35C16.2 4.9 14.21 4 12 4c-4.42 0-7.99 3.58-7.99 8s3.57 8 7.99 8c3.73 0 6.84-2.55 7.73-6h-2.08c-.82 2.33-3.04 4-5.65 4-3.31 0-6-2.69-6-6s2.69-6 6-6c1.66 0 3.14.69 4.22 1.78L13 11h7V4l-2.35 2.35z" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Stretch="Uniform" Width="12" Height="12"/>
                </Button>
                <Button x:Name="SkipButton" ToolTip="Skip task" ToolTipService.InitialShowDelay="0" Foreground="#888888" Style="{StaticResource LinkBtn}" Click="SkipButton_Click" Visibility="Collapsed" Padding="2">
                    <Path Data="M12 4l-1.41 1.41L16.17 11H4v2h12.17l-5.58 5.59L12 20l8-8z" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Stretch="Uniform" Width="12" Height="12"/>
                </Button>
            </StackPanel>
            
            <!-- Resize Grip -->
            <Thumb Grid.Row="0" Grid.RowSpan="3" HorizontalAlignment="Right" VerticalAlignment="Bottom" Cursor="SizeNWSE" Margin="0,0,-8,-8" Opacity="0.4" DragDelta="ResizeThumb_DragDelta">
                <Thumb.Template>
                    <ControlTemplate>
                        <Path Data="M 10,10 L 0,10 L 10,0 Z" Fill="#0A0D12" Width="10" Height="10" Stretch="Fill"/>
                    </ControlTemplate>
                </Thumb.Template>
            </Thumb>
        </Grid>
    </Border>
</Window>
